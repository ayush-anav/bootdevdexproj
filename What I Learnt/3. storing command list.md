# 1. Create a struct so that each command be of that struct command standard
# 2. Put all the commands on an object (map) and write a function that returns all the map at once
```go

func getCommands() map[string]cliCommand {
	return map[string]cliCommand{
		"help": {
			name:        "help",
			description: "Displays a help message",
			callback:    commandHelp,
		},
		"exit": {
			name:        "exit",
			description: "Exit the Pokedex",
			callback:    commandExit,
		},
	}
}
```
###### this is great for our help command as well, we can just print the key and value data

# 3. Each command lives on its own file and the callback handles calling each of the func