# Tee
### tee captures *stdout specifically* — anything written with *fmt.Print, fmt.Println, fmt.Printf,* etc.

---

# It does not capture:

### stderr — error messages (those still only go to the terminal)
### Keyboard input (what you type in)

---