# Keeping state between API calls in GO is better done by sharing a pointer to a struct that holds all the information made from the inital API call!
###### We achieved it here by using a config struct and passing the reference | pointer all along our function calls
###### remember -> communicate address by sharing data 
## Good way to get a quick and dirty GO struct from JSON API is using:
##### [This tool (JSON TO GO)](https://mholt.github.io/json-to-go/)

